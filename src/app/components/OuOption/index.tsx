import * as React from 'react';
import { OptionPropTypes } from '@ui5/webcomponents-react/webComponents/Option';
import { Option } from '@ui5/webcomponents-react';

export function OuOption(props: OptionPropTypes) {
  return <Option {...props}></Option>;
}
