import * as React from 'react';
import { ToolbarProptypes } from '@ui5/webcomponents-react/components/Toolbar';
import { Toolbar } from '@ui5/webcomponents-react';

export function OuToolbar(props: ToolbarProptypes) {
  return <Toolbar {...props}></Toolbar>;
}
